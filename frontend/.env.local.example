NEXT_PUBLIC_API_URL=http://localhost:8000

# Google "Sign in with Google" client ID (from Google Cloud Console). Leave
# empty to disable Google sign-in entirely - the button hides itself.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
